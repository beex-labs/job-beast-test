// Route table — kept tiny; rendering dispatched in App.tsx.
export const routes = {
  home: "/",
  score: "/score",
  result: "/result",
  report: "/report",
  about: "/about",
  privacy: "/privacy",
  tests: "/tests",
} as const;
